[project]
name = "data-profiler"
version = "0.1.0"
description = "Data profiling tool"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-cov>=7.1.0",
]
